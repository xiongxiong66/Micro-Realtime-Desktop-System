# 开发者日志

> 记录进阶方向的完成情况，包含现象、分析、解决方式与当前状态。
> 更新日期：2026-09-09

## 一、进阶测试完成情况

### 1. 输入事件调度优化（已完成）

#### 现象

- 摇杆、键盘高频输入时，事件队列可能出现积压或丢事件。

#### 分析

- `KeyHandle` 深度为 1，`MKey` 使用非阻塞投递，队列满时丢事件并计数。
- `cursorHandle` 深度为 4，桌面/画图循环读取到空、只保留最新坐标，属于事件合并。

#### 解决

- `MKey`：10ms 扫描 + 消抖 + 边沿事件，降低重复触发，记录 `mkey_events/mkey_dropped`。
- `Sw_Adc`：仅在坐标或按键状态变化时投递，记录 `adc_events/adc_dropped`。
- 通过 `while (osMessageQueueGet(...) == osOK)` 把队列读到空，只拿最后一个坐标值，中间帧直接被覆盖。
- 限流：摇杆检测只有在坐标位置发生改变时，才会发送坐标，减小发送频率。
- 桌面、画图循环排空 `cursorHandle` 取最新值，实现事件合并。

#### 状态

已实现。

### 2. 存储一致性保护（已完成）

#### 现象

- 设置、日志写入 W25Q64 过程中断电或复位，可能读到半写、损坏的数据。

#### 分析

- Flash 擦除/写入不是原子操作，需要通过魔数、校验和和字段范围识别数据状态。

#### 解决

- 设置：`SET1` 魔数 + 校验和 + 擦写后回读校验，失败重试并回退默认值。
- 日志：每条 32B 记录带魔数/序号不为 0/校验和，启动时逐条校验；头部损坏时全盘扫描重建；采用 8 条批量写入。
- 图片/音乐文件：文件头魔数校验，损坏文件不会出现在列表中。

#### 状态

已实现。

### 3. 运行负载分析（已完成基础版）

#### 现象

- 需要观察任务状态、栈水位、堆余量、输入事件/丢弃事件/错误计数。

#### 解决

- Monitor 应用显示 UP、SYS、EV/DRP、ERR、HEAP、任务栈 high-water mark。
- 通过链接 map 分析 Flash/RAM 大头，用于决定功能裁剪和编译优化。
- TaskSnap 支持按 `B` 保存任务组合、HEAP、错误、输入事件/丢弃和队列水位的现场快照，具体见“四、近期功能与修复记录”。

### 4. 低功耗调度

#### 解决

- 息屏的时候调用 `portSUPPRESS_TICKS_AND_SLEEP()` 函数，使 CPU 进入睡眠模式，只保留按键扫描与摇杆（200ms）唤醒。
- 无音乐息屏：挂起除 `MKey`/`Sw_Adc` 外全部任务，两者 200ms 轮询。
- 覆盖 `vPortSuppressTicksAndSleep()`：睡眠前 `HAL_SuspendTick()` 停 TIM4，醒来后按实际睡眠 tick 补偿 `uwTick` 再恢复。
- 有音乐息屏保持原逻辑（只挂 `oled`/`Log`，50ms/200ms 轮询），不进入睡眠。

#### 踩坑

- 实现后无法唤醒：CMSIS-RTOS2 的 `SysTick_Handler` 会读 `SysTick->CTRL` 清 COUNTFLAG，先放行中断会导致 tickless 每次只补 1 个 tick，`osDelay(200)` 实际几十秒才到期。修复：醒来后保持中断关闭完成 tick 记账，最后再 `cpsie`。

#### 状态

已实现。

### 5. 任务异常检测（已完成）

#### 现象

- 任务栈溢出、内存分配失败时没有任何检测，系统直接死机且不留痕。

#### 解决

- 开启 `configCHECK_FOR_STACK_OVERFLOW 2`，实现 `vApplicationStackOverflowHook()`。
- 开启 `configUSE_MALLOC_FAILED_HOOK`，实现 `vApplicationMallocFailedHook()`。
- 异常发生时把魔数/错误类型/任务名写入 W25Q64 预留扇区（扇区 1），然后 `NVIC_SystemReset()` 复位。
- 写入前检测 SPI 片选：若 Log 任务正在写 Flash 则跳过写入直接复位，避免破坏数据。
- 开机时 `TaskErr_Init()` 读取该扇区，把上次异常写入日志：`STK xxx` / `MALLOC FAIL`，然后擦除扇区。

#### 分析

- 栈溢出后任务栈已损坏，继续运行不可信，所以采用“记录 + 复位”，不冒险继续执行。
- 不在钩子中直接写日志环形区：正常日志还依赖头部、当前扇区/偏移、擦除等状态，故障上下文与 Log 任务并发会破坏整个环形区；专用扇区是固定地址、固定长度、无头部更新的最小风险写入。
- 仅在 SPI 片选空闲时写入，避免与正在进行的 Flash 传输硬撞。
- 记录先落专用扇区，开机再由 Log 任务转写进日志，既保证留痕又不会污染日志结构。

#### 状态

已实现，待注入测试。

### 6. 任务心跳与卡死检测（已完成）

#### 解决

- 新增 `TaskWatch`，监控范围扩大到 6 个任务：`MKey`、`Sw_Adc`、`Log`、`Oled`、`App_File`、`MusicPlay`，各任务在运行循环里更新心跳。
- `Watchdog` 任务每秒执行一次 `TaskWatch_Check()`，心跳超过 5s 未更新判定卡死，写入日志 `HANG xxx` 并累加 Monitor 错误计数。
- 同一任务只记录一次，恢复心跳后自动清除标记。
- 检查门限的“读时间 + 写时间”放在临界区中，保证同一秒只放行一次检查。
- **挂起状态的任务不参与检测**（息屏挂起 `Log`、Monitor 挂起 `Sw_Adc` 等属正常状态），不会误报。
- 每个任务带“检查开关”：正常运行/处理事件时开启，进入合法等待时关闭；TaskWatch 只检查开关开启且心跳超时的任务。
- UI 按键等待统一走 `Ui_KeyGet()`，等待前关闭检查，收到按键后重新开启并刷新心跳。
- Monitor 设备页两列显示 6 个任务心跳状态（`OK`/`HANG`），挂起/正常阻塞显示 `OK`。

#### 状态

已实现，待注入测试。

### 7. 看门狗与监控任务（已完成）

#### 解决

- 新增 `Watchdog` 任务（`osPriorityAboveNormal`，128×4 栈），循环执行 `TaskWatch_Check()` + `HAL_IWDG_Refresh()` + 200ms 延时。
- IWDG：LSI 40kHz / 64 = 625Hz，重载 1250，超时约 2 秒。
- 优先级高于普通任务，普通任务死循环/饿死不会挡住喂狗；IWDG 作为系统级冻结（中断全关、更高优先级死循环）的兜底。
- 异常复位带确认与留痕：首次检测到 HANG 时先喂狗并写日志；下一轮仍 HANG 则停止喂狗，等待 IWDG 复位；异常恢复则取消复位。
- 开机检查 `RCC_FLAG_IWDGRST`，看门狗复位时写日志 `WDG RESET` 并清标志。
- 息屏深睡眠不挂起 `Watchdog` 任务，避免正常睡眠期间误复位。
- 任务创建放在 `main.c` 的 USER CODE 区，CubeMX 重新生成不会丢失；`.ioc` 已同步 IWDG 外设。

#### 状态

已实现，待注入测试。



## 二、调试与稳定性

### 测试记录

| 测试项 | 结果 | 备注 |
|--------|------|------|
| 高频输入 | 通过 | 事件无积压 |
| 前后台并发 | 通过 | 音乐、日志等后台功能与前台功能可并发且互不影响 |
| 息屏深睡眠唤醒 | 通过 | 按键/摇杆唤醒、音乐息屏不睡眠、 |
| 任务异常注入 | 通过 | 注入项：栈溢出、内存分配失败；预期行为：写 ERR1 预留扇区 → 复位 → 开机转写 `STK xxx` / `MALLOC FAIL` 日志 |任务心跳检测配合看门狗进行检测|
| 资源与持久化 | 通过 | 日志与绘图接近上限时依旧能够正常运行 |
| 长时间运行 | 通过 | 长时间运行后系统依然能够正常运行 |
| 异常注入 | 待补充 | 输入设备断开，桌面显示断开 |




## 三、开发过程遇到的问题

### 问题记录

| 问题 | 原因 | 解决 |
|------|------|------|
| 摇杆坐标队列丢弃事件过多 | 队列深度不足、发送频率过高 | 增长队列；仅坐标变化时发送，降低发送频率 |
| 文件重命名过程程序卡死 | `oled` 任务栈深度不足 | 增加 `oled` 任务栈深度 |
| 音乐播放时摇杆乱飘 | 蜂鸣器供电干扰其他模块 | 蜂鸣器 VCC 与其他模块 VCC 隔离 |
| tickless 实现后无法唤醒 | CMSIS `SysTick_Handler` 读 `SysTick->CTRL` 清 COUNTFLAG，导致 tick 少算 | 醒来后先完成 tick 记账，再放行中断 |
| 按 `A` 偶发卡住 | OLED 与 `MKey` 并发使用 I2C1，传输中途被挂起 | I2C1 增加互斥锁，串行化访问 |
| 进入 Log / 确认页卡死 | 日志查看器读 Flash 与 Log 任务写 Flash 并发使用 SPI1 | `sflash.c` 增加互斥锁，串行化访问 |
| 异常记录依赖备份寄存器 | VBAT 未接，复位后 BKP 数据不保留 | 改为写入 W25Q64 预留扇区（扇区 1），开机读取后写日志并擦除 |
| File 新建图片后光标卡住/保存报错复位 | 画图编辑器在 File 任务上下文运行不正常 | 改为先命名、确认后直接创建空白图片，不再进入编辑器 |
| 日志查看器只能看最新 24 条（4 页） | RAM 缓存上限 `LOG_VIEW_MAX` | 翻页时直接从 Flash 读取，最多 4758 页 |
| 演奏接入后歌曲播放错乱 | MusicPlay 任务被演奏模式线程标志/旧后台状态干扰 | 进入歌曲前调用 `Music_Bg_StopAll()`；演奏音符复用原 `BG_FLAG`，恢复旧唤醒逻辑 |
| W25Q64 音乐数据被旧测试覆盖 | 多次测试写入损坏了原示例曲目 | 临时整区擦除音乐分区，重新写入示例曲目并回读校验（验证后已删除） |

### 1. W25Q64 并发保护（已完成）

#### 现象

- 进入 Log 应用、删除/确认页面时偶发卡死。

#### 分析

- 日志查看器翻页时直接读 Flash，与 Log 任务写 Flash 并发使用 SPI1，总线/传输状态被抢坏。
- 删除/确认流程前后的 Flash 读取同样与 Log 任务并发。

#### 解决

- `sflash.c` 所有读/写/擦除操作加 FreeRTOS 互斥锁，串行化 SPI1 访问。
- `SFlash_SaveSector` 的擦除 + 写入在同一把锁内完成，避免嵌套死锁。
- 故障钩子（TaskErr）绕过锁直接调 BSP 接口，保留 CS 电平检查，避免故障上下文使用 RTOS API。

#### 状态

已实现，待回归。

### 2. I2C 并发保护（已完成）

#### 现象

- 偶尔按 `A` 强制息屏时系统卡住数秒。

#### 分析

- OLED 任务刷屏（I2C1）期间被 `MKey` 抢占，`OLED_Off()` 撞上 HAL `BUSY`。
- 随后挂起 OLED 任务时它卡在 I2C 传输中间，总线被拉住，唤醒时 `OLED_On()` 反复超时。

#### 解决

- I2C1 增加 FreeRTOS 互斥锁，OLED 相关操作串行化，`MKey` 等待时让出 CPU。
- 锁在调度器启动后生效，开机初始化 OLED 不受影响。

### 3. Draw 画笔模式（已完成）

- `C` 循环切换 `DOT`（画点）/ `LINE`（画线）/ `ERASE`（区域擦除）。
- 光标形状随模式变化：实心方块 / 十字准星 / 空心方框。
- 画线与擦除均为两段式：第一点定起点，移动时显示预览，第二点确认。

### 4. 日志实时翻页（已完成）

#### 现象

- 日志查看器只显示最新 24 条（4 页），Flash 中大量历史日志无法查看。

#### 解决

- 删除 RAM 缓存，翻页时 `Log_ReadPage()` 直接从 Flash 按页读取 6 条。
- 日志总数在内存维护并写入 Flash 头部 `total`，旧头部自动补扫一次。
- 最多 4758 页；新增图片操作日志类型 `D`（`NEW`/`RENAMED`/`MODIFIED`/`DELETED`）。

### 5. 输入设备连接检测（已完成）

#### 现象

- 需要判断摇杆模块是否连接，断开时能提示并留痕。

#### 解决

- X/Y 各取最近 100 次采样平均值，两轴平均值持续偏向左下角（相对校准中心偏移超过 500）判定断开。
- 桌面左上角显示 `IN YES/NO`，Monitor 设备页显示 `JOY OK/FAIL`。
- 断开/恢复各写一次日志：`JOY OFF` / `JOY ON`。


## 四、近期功能与修复记录



### 1. 锁屏与唤醒（已完成，待现场回归）

#### 现象

- 按 `A` 息屏后唤醒直接恢复原页面，没有密码保护。
- 自动超时息屏缺少可配置的锁屏延时时长。

#### 解决

- 按 `A` 息屏后唤醒一定进入 PIN 登录。
- 自动超时息屏只有在本次睡眠达到 SET 中 `LOCK` 时长后才锁屏。
- `LOCK` 提供 45s/60s/120s，原息屏时长 10-30s 不变。
- 息屏时保存 OLED 画面，锁屏成功后恢复画面并恢复原任务上下文。
- 原 `defaultTask` 改为锁屏等待任务，不再参与普通空转。
- FreeRTOS 堆由 7000B 调整为 6500B。

#### 状态

已实现

### 2. A 息屏后事件丢弃优化（已完成）

#### 现象

- 按 `A` 息屏后 DRP 增大较多。

#### 分析

- 息屏后 UI 任务挂起，MKey/Sw_Adc 仍向队列投递；若唤醒后需要锁屏，队列在锁屏完成前没有消费者。

#### 解决

- `Screen_Sys_IsLocking()` 标识锁屏请求/锁屏中。
- 锁屏未完成时 MKey 不投递唤醒键，Sw_Adc 不投递光标事件。
- PIN 输入循环每 20ms 排空光标队列，错误锁定页定时排空输入队列。

#### 状态

已实现

### 3. TaskWatch 唤醒误判修复

#### 现象

- RTC 设置页偶尔按 `4/6` 调整时间触发复位，且复位后无 `STK/HANG/WDG` 日志。

#### 分析

- Oled 长时间阻塞等按键时心跳较旧，单靠任务状态判断容易把“正常等待”和“真异常”混淆。

#### 解决

- TaskWatch 改为“检查开关 + 心跳”模型，不再依赖 `eRunning/eReady/eBlocked` 推断。
- UI 等按键统一走 `Ui_KeyGet()`：等待前关闭检查，收到按键后开启检查并立即刷新心跳。
- 息屏恢复任务时刷新对应心跳，但不强制打开本就处于合法等待任务的开关。

#### 状态

已实现，待现场回归。

### 4. LOG 菜单与 TaskSnap 任务快照

#### 解决

- LOG 应用进入后先显示 `LOG VIEW / TASK VIEW`。
- 任意正常页面按 `B` 保存一次任务快照到 W25Q64 扇区 3-10，环形保留最近 8 次。
- 快照包含任务组合、任务状态、栈剩余，以及 HEAP/ERR/EV/DRP/队列水位。
- TASK VIEW 默认停在最新一次 B 记录，通过 `4/6` 切换历史快照。
- TASK VIEW 按 `0` 可删除当前快照，空出的预留扇区由后续 B 快照复用。

#### 踩坑

- 原查看器把整份任务记录放到 Oled 栈，触发栈溢出复位；已改为按行读取单条记录。

### 5. 运行负载首轮实测记录（第一轮）

不同页面停留后按 `B` 保存 TaskSnap：

| 快照 | 场景 | EV | DRP | HEAP | 队列 | 关键栈余量 |
|------|------|----|----|------|------|-----------|
| #52 | Monitor | 110 | 4 | 624 | 0/0/0 | MKey 72、Log 84、Oled 152、Sw_Adc 212 |
| #56 | LOG/FILE | 110 | 4 | 624 | 0/0/0 | MKey 72、Log 84、App_File 180 |
| #59 | Draw | 143 | 4 | 624 | 0/0/0 | MKey 72、Log 84、Oled 152 |
| #63 | SET | 152 | 4 | 624 | 0/0/0 | MKey 72、Log 84、Oled 152 |
| #65 | Music | 225 | 4 | 624 | 0/0/0 | MKey 72、Log 84、Oled 152 |

结论：

- HEAP 各场景稳定为 624B，接近动态内存低位，但未持续下降。
- EV 增长到 225 时 DRP 仍为 4，说明输入没有持续积压。
- CUR/KEY/LOG 队列均为 0，事件处理跟得上。
- MKey 栈余量约 72B 是最接近溢出的任务，B 快照写 Flash 也运行在 MKey 上下文，属于最高风险点。
- Log 约 84B、Oled 约 152B，也需要继续观察。

#### 状态

第一轮负载数据已记录，待补齐 60 分钟长跑和上限/异常注入数据。

### 6. 按键 9 调试挂起注入（已实现）

#### 解决

- 正常页面按 `9` 时，MKey 根据 `App_File` 是否运行选择 Oled 或 File 作为注入目标。
- 目标任务收到按键后，在下一次 `TaskWatch_Beat()` 中进入死循环，不再刷新心跳。
- MKey 同时立即请求 TaskWatch 记 HANG，并强制日志刷盘，避免复位前日志只停留在 RAM/队列。
- 命名、密码、登录和锁屏页禁用该注入。
- TaskWatch 超时后写 `HANG Oled` 或 `HANG File`，看门狗随后停止喂狗并复位系统。

#### 状态

已实现，用于验证任务异常检测与看门狗复位链路。

## 五、待办与风险


- 伪 OTA/真 OTA 未选择实现；如验收需要，需补充版本状态与更新保护。



