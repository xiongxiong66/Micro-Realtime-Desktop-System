# 开发者日志

> 记录进阶方向的完成情况，包含现象、分析、解决方式与当前状态。
> 更新日期：2026-08-24

## 1. 输入事件调度优化（已完成）

### 现象

- 摇杆、键盘高频输入时，事件队列可能出现积压或丢事件。

### 分析

- `KeyHandle` 深度为 1，`MKey` 使用非阻塞投递，队列满时丢事件并计数。
- `cursorHandle` 深度为 4，桌面/画图循环读取到空、只保留最新坐标，属于事件合并。


### 解决

- `MKey`：10ms 扫描 + 消抖 + 边沿事件，降低重复触发，记录 `mkey_events/mkey_dropped`。
- `Sw_Adc`：仅在坐标或按键状态变化时投递，记录 `adc_events/adc_dropped`。
-通过while (osMessageQueueGet(...) == osOK) 把队列读到空，只拿最后一个坐标值，中间帧直接被覆盖。
-限流：摇杆检测只有在坐标位置发生改变时，才会发送坐标，减小发送频率
- 桌面、画图循环排空 `cursorHandle` 取最新值，实现事件合并。

### 状态

已实现。

## 2. 存储一致性保护（已完成）

### 现象

- 设置、日志写入 W25Q64 过程中断电或复位，可能读到半写、损坏的数据。

### 分析

- Flash 擦除/写入不是原子操作，需要通过魔数、校验和和字段范围识别数据状态。

### 解决

- 设置：`SET1` 魔数 + 校验和 + 擦写后回读校验，失败重试并回退默认值。
- 日志：每条 32B 记录带 魔数/序号不为0/校验和，启动时逐条校验；头部损坏时全盘扫描重建；采用 8 条批量写入。
- 图片/音乐文件：文件头 魔数 校验，损坏文件不会出现在列表中。

### 状态

已实现。

## 3. 运行负载分析（已完成基础版）

### 现象

- 需要观察任务状态、栈水位、堆余量、输入事件/丢弃事件/错误计数。

### 解决

- Monitor 应用显示 UP、SYS、EV/DRP、ERR、HEAP、任务栈 high-water mark。
- 通过链接 map 分析 Flash/RAM 大头，用于决定功能裁剪和编译优化。

### 状态

已实现


记录开发过程遇到的问题

摇杆发送坐标队列丢弃事件过多，通过增长队列，降低发送频率，坐标改变才发送解决事件丢弃严重的问题
文件重命名过程出现程序卡死，经检测增加oled任务的栈深度使得功能恢复正常
音乐播放时，摇杆乱飘，将蜂鸣器的vcc与其他模块vcc隔离，保证其他模块稳定运行

记录测试
高频输入:事件无积压
前后台并发测试：音乐，日志等功能与前台功能可并发进行，并且互不影响
资源与持久化测试：
长时间运行测试：
异常注入测试


